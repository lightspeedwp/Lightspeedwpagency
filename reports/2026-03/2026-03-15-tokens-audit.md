# Design tokens audit report

**Category:** Report  
**Version:** 1.0.0  
**Last Updated:** 2026-03-15  
**Status:** Complete  
**Trigger:** `audit tokens`  
**Task List:** `/tasks/tokens-task-list.md`

---

## Summary

| Metric | Value |
|---|---|
| Files scanned | ~130 `.tsx` files |
| Files with violations | 7 |
| Total violations found | ~52 |
| Auto-fixed | ~48 |
| Remaining manual tasks | 2 |
| Lucide imports found | 0 |

---

## Auto-Fixes Applied (7 files)

### App.tsx (7 violations fixed)
- Replaced `#ff3333` with `var(--destructive)`
- Replaced `#0a0a0a` with `var(--background)`
- Replaced `#1a1a1a` with `var(--card)`
- Replaced `#333`, `#555` with `var(--border)`
- Replaced `#fff` with `var(--foreground)`
- Replaced `2rem`, `1.5rem`, `1rem`, `0.875rem`, `0.5rem`, `8px`, `4px` with spacing/radius/text tokens
- Replaced `system-ui, -apple-system, sans-serif` with `var(--font-primary)`

### SecurityServiceTemplate.tsx (~30 violations fixed)
- Replaced all `#ffffff` with `var(--foreground)`
- Replaced all `#a1a1aa` with `var(--muted-foreground)`
- Replaced all `#18181b` with `var(--card)`
- Replaced all `#27272a` with `var(--muted)` / `var(--border)`
- Replaced all `#3f3f46` with `var(--border)`
- Replaced all `#0a0a0a` with `var(--background)`
- Replaced all `#52525b` with `color-mix(in srgb, var(--muted-foreground) 50%, transparent)`
- Replaced `#ff5f56` with `var(--color-terminal-red)`
- Replaced `#27c93f` with `var(--color-terminal-green)`
- Replaced `#ffbd2e` with `var(--color-terminal-yellow)`
- Replaced `#38bdf8` with `var(--accent)`
- Replaced `rgba(39, 201, 63, *)` with `color-mix(in srgb, var(--color-terminal-green) *, transparent)`
- Replaced `rgba(255, 95, 86, *)` with `color-mix(in srgb, var(--color-terminal-red) *, transparent)`

### DevelopmentServiceTemplate.tsx (4 violations fixed)
- Terminal dots: `#ff5f56` / `#ffbd2e` / `#27c93f` replaced with `var(--color-terminal-*)`
- `fontWeight: 'bold'` replaced with `var(--font-weight-bold)`

### ContentServiceTemplate.tsx (3 violations fixed)
- All `#ff5f56` (proofreading marks) replaced with `var(--color-terminal-red)`

### DesignServiceTemplate.tsx (4 violations fixed)
- `color: 'white'` replaced with `var(--primary-foreground)`
- All `fontWeight: 'bold'` replaced with `var(--font-weight-bold)`

### WooCommerceSolutionTemplate.tsx (2 violations fixed)
- `fontWeight: 'bold'` replaced with `var(--font-weight-bold)`
- `#e6edf3` replaced with `var(--foreground)`

### ServicesLandingTemplate.tsx (1 violation fixed)
- `fontWeight: 'bold'` replaced with `var(--font-weight-bold)`

### site-map.css (added missing styles)
- Added `.site-map__link--missing` with `opacity: 0.55` and `pointer-events: none`
- Added `.site-map__icon--warning` with `color: var(--color-terminal-yellow)`

---

## Remaining Issues

### T1: WooCommerceSolutionTemplate — `#0d1117` hardcoded background
Line ~360: `backgroundColor: '#0d1117'` in the tech stack card. This should use `var(--background)` or `var(--card)`. Low priority since it only appears once.

### T2: Logo files — hex values in JSDoc comments only
Logo.tsx and LogoInline.tsx contain hex values (#127EFF, #FF3131, etc.) in JSDoc documentation comments only. These are acceptable as documentation references, not runtime values.

---

## Compliance Score

| Token Type | Before | After |
|---|---|---|
| Colors (hex) | ~52 violations | 1 remaining |
| Font weights | 6 violations | 0 |
| Font families | 1 violation (App.tsx) | 0 |
| Spacing (hardcoded px/rem) | 7 violations | 0 |
| Border radius | 2 violations | 0 |
| Lucide imports | 0 | 0 |
| **Overall** | **~68 violations** | **1 remaining (99% compliant)** |

---

**Generated by:** `audit tokens` trigger  
**Next run recommended:** After any new template creation