# Accessibility (a11y) Audit Report

**Date:** 2026-03-15  
**Trigger:** `audit a11y`  
**Task List:** `/tasks/a11y-task-list.md`

---

## Summary

| Metric | Value |
|---|---|
| WCAG Level | AA Target |
| Files scanned | ~130 `.tsx`, ~220 `.css` |
| Issues found | 8 |
| Auto-fixed | 4 |
| Remaining | 4 |

---

## Compliance Overview

### Passing (WCAG 2.1 AA)

| Criterion | Status | Evidence |
|---|---|---|
| 1.1.1 Non-text content | ✅ Pass | All `<img>` tags have `alt` attributes |
| 1.3.1 Info & Relationships | ✅ Pass | Semantic HTML, proper heading hierarchy |
| 2.1.1 Keyboard | ✅ Pass | All interactive elements are `<button>` or `<a>` |
| 2.4.1 Bypass Blocks | ✅ Pass | SkipLink component exists, targets `#main-content` |
| 2.4.2 Page Titled | ✅ Pass | Route-based titles via Helmet/document.title |
| 2.4.6 Headings & Labels | ✅ Pass | Sequential H1→H2→H3, single H1 per page |
| 2.5.5 Target Size | ✅ Pass | Touch target guidance in design tokens |
| 3.1.1 Language | ✅ Pass | `lang` attribute on `<html>` |
| 4.1.2 Name, Role, Value | ✅ Pass | ARIA labels on icon-only buttons |

### Needs Improvement

| Criterion | Status | Issues |
|---|---|---|
| 1.3.1 Landmarks | ⚠️ Fixed | Nested `<main>` elements (3 files) |
| 2.3.1 Motion | ⚠️ Partial | `prefers-reduced-motion` in 7 CSS files, but not all animation files |
| 2.4.7 Focus Visible | ⚠️ Partial | `:focus-visible` in 8 CSS files, needs wider coverage |

---

## Auto-Fixes Applied (4)

### A0.1: Nested `<main>` landmarks — ComplianceScorecard.tsx
Changed 3 conditional `<main>` wrappers to `<div>` since component renders inside RootLayout's `<main>`.

### A0.2: Nested `<main>` landmark — TourOperatorSolutionTemplate.tsx
Changed `<main>` wrapper to `<div>` to avoid nested landmark.

### A0.3: Nested `<main>` landmark — GenericPolicyTemplate.tsx
Changed nested `<main>` to `<div role="region" aria-label="Policy content">`.

### A0.4: Nested `<main>` landmark — SnippetGeneratorTemplate.tsx
Changed `<main>` to `<div role="region" aria-label="Generated snippet output">`.

---

## Remaining Issues

### A1.1 (P2): Extend `prefers-reduced-motion` coverage
Currently only 7 CSS files include `prefers-reduced-motion`. The following animation-heavy files need it:
- `advanced-animations.css`
- `micro-interactions.css`
- `scroll-down.css`
- Template-specific animation keyframes

### A1.2 (P2): Extend `:focus-visible` to all interactive elements
Currently 8 CSS files define `:focus-visible`. Need to add global focus ring styles for:
- All `.snippet-gen__tab`, `.snippet-gen__preset` buttons
- All template-specific interactive cards
- Solution page CTA links

### A1.3 (P3): Add `aria-live` to dynamic content
- SnippetGeneratorTemplate: Code output should announce changes via `aria-live="polite"`
- ComplianceScorecard: Test results should announce completion

### A1.4 (P3): Colour contrast on terminal UIs
Terminal-themed sections (Security, Development templates) use green-on-dark text that may not meet 4.5:1 ratio with some background combinations. Verify with contrast checker.

---

## Score: B+

Strong foundations (skip links, semantic HTML, keyboard support, ARIA labels). Primary gaps are in animation reduction coverage and focus ring consistency across newer components.

---

**Generated by:** `audit a11y` trigger
