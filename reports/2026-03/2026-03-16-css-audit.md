# CSS architecture audit — March 16, 2026

**Category:** Report  
**Version:** 1.0.0  
**Last Updated:** 2026-03-16  
**Status:** Complete  
**Trigger:** `audit css`

---

## Summary

- **CSS files scanned:** ~220+
- **Hardcoded hex in CSS:** 0 genuine violations (all are variable definitions, CSS fallbacks, print stylesheet, or comments)
- **Hardcoded font-family in CSS:** 0 violations (all use `var(--font-*)`)
- **Architecture grade:** A-
- **Import chain:** Clean — all imports resolve correctly after P4.3 fixes

---

## Hex Values in CSS — Classification

| Location | Count | Classification |
|---|---|---|
| theme-dark-colors.css, theme-light-colors.css | 50+ | ✅ Variable definitions (source of truth) |
| theme-funky.css | 8 | ✅ CSS fallback values `var(--neon-color, #7BE7FF)` |
| journey-phase-indicator.css | 3 | ✅ CSS fallback values `var(--node-accent, #8b5cf6)` |
| featured-project-text.css | 1 | ✅ CSS fallback value `var(--color-black, #000)` |
| print.css | 5 | ✅ Print stylesheet (intentionally hardcoded for consistent print output) |
| route-loading-fallback.css | 3 | ✅ Comments only |

**Zero genuine violations.** All hex values are either variable definitions, fallback values, print stylesheet values, or comments.

---

## Font-Family in CSS

All CSS `font-family` declarations use `var(--font-primary)`, `var(--font-secondary)`, or `var(--font-mono)`. Zero hardcoded font names.

---

## Import Chain Health

All CSS imports resolve correctly. The P4.3 CSS import optimization (completed March 16) fixed all broken import paths. The new `development-service.css` added in the `apply bem` pass is properly registered in `index.css`.

---

## Token Gap Filled

Added `--line-height-comfortable: 1.6` to typography system (see tokens audit report for details). This fills the gap between `--line-height-normal` (1.5) and `--line-height-relaxed` (1.75) that 50+ files were using via hardcoded values.

---

## Recommendations

1. No immediate CSS fixes needed
2. Future `apply bem` passes should move inline `lineHeight: 1.6` → BEM CSS using `var(--line-height-comfortable)`
3. Future `apply bem` passes should move inline `letterSpacing: '0.05em'` → BEM CSS using `var(--letter-spacing-wider)`