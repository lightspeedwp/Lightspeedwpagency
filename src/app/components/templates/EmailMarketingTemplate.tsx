/**
 * Email Marketing Template
 * 
 * Redirects to Newsletter Service Template as they serve the same purpose.
 */

import { NewsletterServiceTemplate } from './NewsletterServiceTemplate';

export function EmailMarketingTemplate() {
  return <NewsletterServiceTemplate />;
}
